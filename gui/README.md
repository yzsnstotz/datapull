# Datapull GUI

Datapull 的图形用户界面，基于 Vue3 + Vite + Pinia 构建。

## 功能特性

- 📊 **数据源管理**：创建、编辑、删除数据源配置
- 🚀 **任务控制**：启动、停止爬取任务，实时查看任务状态
- 📤 **上传中心**：查看分片列表，批量上传到 DriveQuiz
- 📝 **日志查看**：实时查看系统、爬取、上传日志
- 💻 **系统状态**：监控系统资源使用情况

## 快速开始

### 安装依赖

```bash
cd gui
pnpm install
```

### 开发模式

```bash
# 启动前端开发服务器（端口 5173）
pnpm dev

# 在另一个终端启动后端API服务器（端口 3100）
cd ..
pnpm api:dev
```

### 构建

```bash
pnpm build
```

构建后的文件将输出到 `gui/dist` 目录。

## 项目结构

```
gui/
├── src/
│   ├── components/      # 通用组件
│   │   ├── DataTable.vue
│   │   ├── LogViewer.vue
│   │   └── StatCard.vue
│   ├── pages/           # 页面组件
│   │   ├── SourcesPage.vue
│   │   ├── TasksPage.vue
│   │   ├── UploadPage.vue
│   │   ├── LogsPage.vue
│   │   └── SystemPage.vue
│   ├── services/        # API 服务
│   │   └── datapullApi.ts
│   ├── store/           # Pinia 状态管理
│   │   └── guiStore.ts
│   ├── types/           # 类型定义
│   │   └── common.ts
│   ├── App.vue          # 根组件
│   ├── main.ts          # 入口文件
│   └── router.ts        # 路由配置
├── index.html
├── vite.config.ts
└── package.json
```

## API 接口

GUI 通过 REST API 和 WebSocket 与后端通信：

- **REST API**：`/api/*`
- **WebSocket**：`/ws`

详细接口规范请参考 `docs/GUI/《📐 Datapull GUI 参数与接口规范 v1.1》`。

## 环境变量

GUI 相关的环境变量在项目根目录的 `.env` 文件中配置：

```bash
DATAPULL_PORT=3100          # API 服务器端口
GUI_AUTH_ENABLED=false       # 是否启用 Basic Auth
GUI_BASIC_USER=admin         # Basic Auth 用户名
GUI_BASIC_PASS=admin123      # Basic Auth 密码
```

## 开发说明

### 添加新页面

1. 在 `src/pages/` 创建新的 Vue 组件
2. 在 `src/router.ts` 中添加路由配置
3. 在 `src/App.vue` 的导航菜单中添加链接

### 添加新组件

在 `src/components/` 目录下创建新的 Vue 组件，并在需要的地方导入使用。

### 状态管理

使用 Pinia 进行状态管理，所有全局状态定义在 `src/store/guiStore.ts` 中。

## 技术栈

- **Vue 3**：渐进式 JavaScript 框架
- **Vite**：下一代前端构建工具
- **Pinia**：Vue 的状态管理库
- **Vue Router**：Vue 的官方路由管理器
- **Axios**：HTTP 客户端

## 许可证

MIT

