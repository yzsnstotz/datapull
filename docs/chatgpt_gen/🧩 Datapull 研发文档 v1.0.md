非常好 ✅
下面为你输出 **《🧩 Datapull 研发文档 v1.0》**，
它是贯穿整个项目执行阶段的**核心研发蓝图**，以《产品文档》为逻辑依据，明确每个功能、方法、流程与对应的技术实现思路。

此文档可直接作为：

* O1 / Cursor 的研发任务输入文件
* 项目主仓库 `/docs/datapull/` 下的主文档
* PR / MR 审查基准文件

---

# 🧩 《Datapull 研发文档 v1.0》

**版本**：v1.0
**发布日期**：2025-01-06
**依据文档**：《📘 Datapull 产品文档 v1.0》
**配套文档**：《📐 参数与接口规范》《✅ 工作核对清单》《🧾 进度同步模版》

---

## 一、研发目标

### 🎯 核心使命

研发一个可独立运行的 **数据抓取 + 清洗 + 分片 + 上传** 本地服务，
使 DriveQuiz 系统的 RAG 知识库实现自动化更新，覆盖多语言（尤其是日语）内容。

### 🧩 功能要点

1. 从指定源（政府 / 行业组织 / 教育机构）抓取驾驶相关资料；
2. 清理并提取纯净正文；
3. 自动分片以适配向量化结构；
4. 将分片内容上传至 DriveQuiz RAG API；
5. 完整记录操作、错误与性能日志。

---

## 二、总体设计架构

### 🏗️ 模块化分层结构

```
Crawl (爬取)
 ├─> Extract (提取)
 │     ├─ HTML / PDF Parser
 │     └─ 内容清理与去噪
 ├─> Chunk (分片)
 │     └─ 按语义切割文本
 ├─> Ingest (入库)
 │     ├─ DriveQuiz API 上传
 │     ├─ 批量处理 / 重试
 │     └─ 操作记录
 ├─> Services (业务编排)
 │     └─ 流程调度、日志记录
 └─> Utils (工具)
       └─ 配置验证、哈希、日志
```

---

## 三、功能研发明细（按模块）

### 1️⃣ Crawler 模块

**文件夹**：`src/crawler/`
**目的**：负责从 sources.json 中定义的 URL 拉取 HTML/PDF 内容。

#### 🧠 主要文件与方法

| 文件              | 方法                             | 功能说明                                                |
| --------------- | ------------------------------ | --------------------------------------------------- |
| `fetcher.ts`    | `fetchPage(task: CrawlTask)`   | 发起 HTTP 请求，识别 Content-Type（HTML/PDF），校验 robots.txt。 |
|                 | `checkRobots(url: string)`     | 判断目标网站是否允许爬取。                                       |
| `discoverer.ts` | `discoverLinks(html, baseUrl)` | 从 HTML 中发现新的合法链接。                                   |
| `scheduler.ts`  | `startScheduler()`             | 控制爬取任务队列、并发和间隔。                                     |

#### ⚙️ 研发要点

* 使用 **axios** + **p-queue** 控制并发；
* 引入 **robots-txt-parse** 保障合法性；
* 统一错误捕获并返回结构化日志；
* 支持 HTTP Retry（默认3次指数退避）。

---

### 2️⃣ Extractor 模块

**文件夹**：`src/extractors/`
**目的**：将 HTML / PDF 内容解析为纯文本结构体，过滤噪声。

#### 🧠 文件与方法

| 文件                  | 方法                                        | 功能说明                        |
| ------------------- | ----------------------------------------- | --------------------------- |
| `base-extractor.ts` | `extract(url, rawData)`                   | 定义通用接口。                     |
| `html-extractor.ts` | `extractHtml(html: string, url: string)`  | 使用 cheerio 提取正文、标题、去除脚注、广告。 |
| `pdf-extractor.ts`  | `extractPdf(buffer: Buffer, url: string)` | 使用 pdf-parse 提取 PDF 文本。     |

#### ⚙️ 研发要点

* **输出结构**：

  ```ts
  {
    title: string;
    url: string;
    content: string;
    lang: "ja" | "zh" | "en";
    version: string;
    sourceId: string;
  }
  ```
* 自动检测编码（UTF-8 / Shift_JIS）；
* 清理规则：去除 `<script>`、`<footer>`、空行与重复段落；
* 添加元信息：sourceId + version（根据配置生成）。

---

### 3️⃣ Chunker 模块

**文件夹**：`src/chunkers/`
**目的**：将提取后的文本按语义分片，保持上下文连贯性。

#### 🧠 文件与方法

| 文件                  | 方法                                      | 功能说明                          |
| ------------------- | --------------------------------------- | ----------------------------- |
| `chunk-strategy.ts` | `split(text, lang)`                     | 定义分片策略接口。                     |
| `text-chunker.ts`   | `chunkDocument(doc: ExtractedDocument)` | 实现多语言分片策略，输出 DocumentChunk[]。 |

#### ⚙️ 研发要点

* 每个分片长度：**500–800 字符**；
* 分割点优先：句号、换行符；
* 每片保留 **chunkIndex**、**totalChunks**；
* 生成 **SHA256(content)** 作为 contentHash；
* 支持三种策略：

  * `JA_CHUNKER`：基于句号（。！）；
  * `ZH_CHUNKER`：基于换行符；
  * `EN_CHUNKER`：基于句子断句。

---

### 4️⃣ Ingester 模块

**文件夹**：`src/ingesters/`
**目的**：将分片数据上传到 DriveQuiz RAG API。

#### 🧠 文件与方法

| 文件                    | 方法                                    | 功能说明                                         |
| --------------------- | ------------------------------------- | -------------------------------------------- |
| `drivequiz-client.ts` | `uploadBatch(request: IngestRequest)` | 通过 Bearer Token 调用 `/api/v1/rag/docs/batch`。 |
|                       | `uploadSingle(doc: DocumentChunk)`    | 上传单文档片段。                                     |
| `batch-processor.ts`  | `processChunks(docs)`                 | 按 100 条为一组批量上传。                              |
| `retry-handler.ts`    | `retry<T>(fn, times)`                 | 封装统一重试逻辑。                                    |

#### ⚙️ 研发要点

* 默认每批次最多 **100 条文档**；
* 失败项进入重试队列；
* 返回结构统一：

  ```ts
  {
    success: true,
    processed: number,
    failed: number,
    operationId: string
  }
  ```
* 上传成功后 DriveQuiz 自动触发向量化；
* 对 `DUPLICATE_DOCUMENT` 错误进行跳过处理。

---

### 5️⃣ Services 模块

**文件夹**：`src/services/`
**目的**：组织业务逻辑，负责任务编排、日志与监控。

#### 🧠 文件与方法

| 文件                    | 方法                                         | 功能说明                                             |
| --------------------- | ------------------------------------------ | ------------------------------------------------ |
| `crawl-service.ts`    | `startCrawl(sourceId)`                     | 调用 crawler → extractor → chunker → ingester 全流程。 |
| `ingest-service.ts`   | `ingestSource(sourceId, version)`          | 独立执行上传任务。                                        |
| `operation-logger.ts` | `logStart()`, `logSuccess()`, `logError()` | 按 operationId 保存执行结果。                            |

#### ⚙️ 研发要点

* 每个任务生成唯一 `operationId`；
* 日志结构对齐 DriveQuiz `/operations`；
* 支持断点恢复：中断后根据 operationId 恢复执行；
* 输出文件：

  ```
  logs/operations/op_xxx.json
  logs/error.log
  ```

---

### 6️⃣ Utils 模块

**文件夹**：`src/utils/`
**目的**：提供通用工具、验证和日志。

#### 🧠 文件与方法

| 文件             | 方法                       | 功能说明                 |
| -------------- | ------------------------ | -------------------- |
| `logger.ts`    | `createLogger(level)`    | 基于 winston 的结构化日志输出。 |
| `validator.ts` | `validateConfig(file)`   | 使用 zod 校验配置文件结构。     |
| `hasher.ts`    | `sha256(text)`           | 计算内容哈希。              |
| `encoder.ts`   | `detectEncoding(buffer)` | 自动检测文件编码。            |

---

### 7️⃣ CLI 层与脚本

**文件夹**：`scripts/`
**目的**：提供一键执行入口与配置校验脚本。

#### 🧠 主要命令

| 命令                     | 功能              | 示例                     |
| ---------------------- | --------------- | ---------------------- |
| `pnpm crawl`           | 全流程爬取 + 提取 + 上传 | ✅ 自动生成 operationId     |
| `pnpm crawl --dry-run` | 测试模式，不入库        | ✅ 输出预览                 |
| `pnpm validate`        | 校验 sources.json | ✅ schema 检查            |
| `pnpm report`          | 输出统计报告          | 📊 Processed: 120 docs |

---

## 四、执行流程设计（全链路）

### 🧭 流程图

```
sources.json
   ↓
Crawler.fetcher()
   ↓
Extractor.extractHtml() / extractPdf()
   ↓
Chunker.chunkDocument()
   ↓
Ingester.uploadBatch()
   ↓
OperationLogger.logSuccess()
   ↓
DriveQuiz 自动触发向量化
```

### 🧩 异常处理分支

* 网络错误 → 进入 RetryHandler；
* 内容为空 → 写入 error.log；
* 上传失败 → 记录 operationId + failed 数；
* 全局异常 → 触发 graceful shutdown 并保存当前状态。

---

## 五、测试与验证方案

| 测试类别 | 目标       | 工具                | 验收标准      |
| ---- | -------- | ----------------- | --------- |
| 单元测试 | 模块功能正确   | Vitest            | 通过率 ≥ 90% |
| 集成测试 | 各模块联动    | Supertest         | 无阻塞错误     |
| 性能测试 | 100 文档上传 | Node Benchmark    | < 5s      |
| 异常测试 | 异常输入处理   | Mock + Vitest     | 错误码准确     |
| 兼容测试 | 多语言/编码   | Shift_JIS / UTF-8 | 输出一致      |

---

## 六、研发环境配置

### `.env.example`

```bash
DRIVEQUIZ_API_URL=https://dev-api.drivequiz.com/api/v1/rag
DRIVEQUIZ_API_TOKEN=your_token_here
CRAWL_CONCURRENCY=5
LOG_LEVEL=info
CACHE_DIR=./data/cache
```

### `config/sources.json` 示例

```json
[
  {
    "sourceId": "gov_npa_driving",
    "seedUrls": ["https://www.npa.go.jp/bureau/traffic/license/"],
    "include": ["license", "faq"],
    "exclude": ["#"],
    "lang": "ja",
    "version": "2025Q1"
  }
]
```

---

## 七、任务验收标准

| 模块        | 验收指标             | 标准                  |
| --------- | ---------------- | ------------------- |
| Crawler   | 并发爬取 + robots 验证 | 正确率 ≥ 95%           |
| Extractor | 提取准确率            | ≥ 90%               |
| Chunker   | 分片边界准确性          | ≥ 95%               |
| Ingester  | 批量成功率            | ≥ 99%               |
| Service   | 操作记录完整           | 100% 生成 operationId |
| Utils     | 日志可追踪性           | 100% 含 traceId      |

---

## 八、交付内容与目录结构

```
docs/datapull/
 ├── datapull-product-doc.md
 ├── datapull-development-guide.md     ← 当前文档
 ├── datapull-dev-standard.md
 ├── datapull-parameter-spec.md
 ├── datapull-checklist.md
 ├── datapull-progress-template.md
src/
 ├── crawler/
 ├── extractors/
 ├── chunkers/
 ├── ingesters/
 ├── services/
 ├── utils/
 └── main.ts
scripts/
 ├── crawl-and-ingest.ts
 ├── validate-config.ts
tests/
 ├── unit/
 └── integration/
```

---

## 九、版本与维护机制

| 项目    | 内容                           |
| ----- | ---------------------------- |
| 版本标识  | v1.0.0                       |
| 维护周期  | 每季度更新一次                      |
| 版本策略  | SemVer（主版本 / 功能 / 修复）        |
| 日志归档  | logs/ 下每日轮换                  |
| CI/CD | 预留 Render / Railway pipeline |

---

## 十、研发负责人

| 角色             | 职责                     | 姓名 / 工号 |
| -------------- | ---------------------- | ------- |
| PM / Tech Lead | 产品设计与架构决策              | ChatGPT |
| Backend Lead   | Ingester / API 联调      | —       |
| Frontline Dev  | Crawler / Extractor 实现 | —       |
| QA             | 测试与联调验证                | —       |
| Ops            | 环境配置与日志监控              | —       |

---

✅ **说明：**
本《研发文档》是产品与技术的执行衔接文件，
每个开发周期开始前需先在该文档中确认任务范围与实现路径，
提交后与《核对清单》《进度模版》联动使用。

---

是否希望我现在为这 6 份文档（产品 / 研发 / 参数 / 核对 / 模版 / 结构）生成一份自动化索引 README（含目录树与内部跳转链接）？
该 README 可直接放入 `/docs/datapull/README.md`，作为仓库官方文档入口。
